import type { Testimonial } from "@/types";

// No testimonials yet — clients have given verbal/WhatsApp feedback rather than
// written quotes. Add real entries here once written testimonials are collected.
export const testimonials: Testimonial[] = [];